* Parameters used from h5 headers to the PHIL
  * e.g. specifying keys/paths. Will enable custom definitions
  * For convenience, some processing might need a move to a class?

* In main Job class, revisit untracked/unused parameters.
  * It will conflict with the Major PHIL file used in sub-jobs

* Generic job-control Phil entry to __init__
  * nproc